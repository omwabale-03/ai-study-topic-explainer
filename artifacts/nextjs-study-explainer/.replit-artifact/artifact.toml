kind = "web"
previewPath = "/"
title = "AI Study Topic Explainer"
version = "1.0.0"
id = "artifacts/nextjs-study-explainer"
router = "path"

[[integratedSkills]]
name = "react-vite"
version = "1.0.0"

[[services]]
localPort = 22815
name = "web"
paths = ["/"]

[services.development]
run = "pnpm --filter @workspace/nextjs-study-explainer run dev"

[services.env]
PORT = "22815"
BASE_PATH = "/"

[services.production]
build = ["pnpm", "--filter", "@workspace/nextjs-study-explainer", "run", "build"]
publicDir = "artifacts/nextjs-study-explainer/dist/public"
serve = "static"

[[services.production.rewrites]]
from = "/*"
to = "/index.html"
